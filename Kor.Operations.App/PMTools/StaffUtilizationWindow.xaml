<Window x:Class="Kor.Operations.PMTools.StaffUtilizationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Staff Utilization  Rolling 12 Weeks"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource App.Background}"
        Width="980"
        MinWidth="860"
        Height="620"
        MinHeight="480"
        Loaded="Window_Loaded">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource Card}" Padding="10" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="SearchBox"
                         Grid.Column="0"
                         Height="32"
                         VerticalContentAlignment="Center"
                         Padding="8,0"
                         TextChanged="SearchBox_TextChanged"/>
                <Button Grid.Column="1"
                        Content="Close"
                        Width="84"
                        Height="32"
                        Margin="8,0,0,0"
                        Style="{StaticResource Button.Secondary}"
                        Click="CloseBtn_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="8">
            <DataGrid x:Name="UtilGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserSortColumns="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      HorizontalScrollBarVisibility="Disabled">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Employee Name" Binding="{Binding EmployeeName}" Width="200"/>
                    <DataGridTextColumn Header="This Week Hrs" Binding="{Binding WeekHrs, StringFormat={}{0:0.0}}" Width="110"/>
                    <DataGridTextColumn Header="4-Wk Avg Hrs/Wk" Binding="{Binding FourWkAvg, StringFormat={}{0:0.0}}" Width="120"/>
                    <DataGridTextColumn Header="12-Wk Total Hrs" Binding="{Binding TwelveWkHrs, StringFormat={}{0:0.0}}" Width="120"/>
                    <DataGridTextColumn Header="12-Wk Avg Hrs/Wk" Binding="{Binding TwelveWkAvg, StringFormat={}{0:0.0}}" Width="120"/>
                    <DataGridTextColumn Header="Utilization %" Binding="{Binding UtilizationPct, StringFormat={}{0:P0}}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#C2410C"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Status}" Value="High">
                                        <Setter Property="Foreground" Value="#166534"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Status}" Value="Low">
                                        <Setter Property="Foreground" Value="#B91C1C"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StatusBar Grid.Row="2" Margin="0,8,0,0">
            <TextBlock x:Name="StatusText"/>
        </StatusBar>
    </Grid>
</Window>
