<Window x:Class="Kor.Operations.App.BusinessDevelopment.Workspace.JobRunHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding JobName, StringFormat='Job history  {0}'}"
        Width="900"
        Height="500"
        WindowStartupLocation="CenterOwner"
        Loaded="Window_Loaded">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding JobName}"
                   FontWeight="Bold"
                   FontSize="14"
                   Margin="0,0,0,8"/>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Runs}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Started UTC"
                                    Binding="{Binding StartedAtUtc, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}"
                                    Width="170"/>
                <DataGridTextColumn Header="Ended UTC"
                                    Binding="{Binding EndedAtUtc, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}"
                                    Width="170"/>
                <DataGridTextColumn Header="OK"
                                    Binding="{Binding Success}"
                                    Width="60"/>
                <DataGridTextColumn Header="Summary"
                                    Binding="{Binding Summary}"
                                    Width="*"/>
                <DataGridTextColumn Header="Error"
                                    Binding="{Binding Error}"
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2"
                   Text="{Binding StatusMessage}"
                   Foreground="Gray"
                   Margin="0,8,0,0"/>
    </Grid>
</Window>
