# BD Module Audit — 2026-06-19 (Codex audit + Claude verification)

Independent Codex audit (per `docs/BD-Audit-Prompt-2026-06-19.md`), then each
finding re-verified by Claude against the live DB/code. Severities below are
Claude's calibration after verification.

## Summary
- Codex raised **1 Critical / 6 Major / 4 Minor**. All confirmed real on
  re-verification; two reframed (M2/M4 lighter than Major; M3 automation-race
  overstated — the dedup job is a hard no-op so the scheduled trigger can't race).
- Top gates before the AI (Arc-7) / Deltek-fusion phase: **C1 CRM dedup +
  identity**, **M5 CA funnel stage filtering**, **M1 person identity key**.

## Findings (Codex → Claude verdict → evidence)
| # | Finding | Verdict | Key evidence (verified) |
|---|---|---|---|
| C1 | CRM duplicate engagements per buyer | ✅ Real (top) | Ledcor 31/76 (Omar) + 35/77 (Islam) exact dup pairs; Duke 32/63 mis-regioned. Root: `BdTrackingImport` blind insert (`SqlCrmEngagementStore.cs:93-110`). **Dedup key must be buyer+owner+region, not buyer alone.** |
| M1 | IntelPerson name-only NaturalKey | ✅ Real | `IntelNaturalKey.cs:7-16` = SHA1(normalize(name)); `UQ_IntelPerson_NaturalKey` (Schema/64:30). Two same-name people collide (hit live: John Wu). |
| M2 | 2 affiliations → retired enrichment | ✅ Real, **Minor** | Exactly 2 rows (13739 CEI/4552, 19435 Arcadis/153); enrichment-orgs retired 2026-06-16; live org FK correct. |
| M3 | No app-lock; trigger "can race" | ⚠️ **Half** | `sp_getapplock`=0 (true). But `CanonicalOrgDedupJob` is hard no-op → Sunday trigger fires inert; **no automation race**. Residual = manual CLI runs only → Minor. |
| M4 | 2500-row dedup allowlist | ✅ Count exact → **architectural** | Intentional merge ledger from firm-family campaigns; not a bug. Split per-campaign w/ validation. |
| M5 | CA/SF admits late-stage permits | ✅ Real & material | `CA_SocrataSF` where = units/value + `'%new construction%'`, no status filter. Active SF: 159 filed vs 113 complete + 64 withdrawn + 59 expired + 10 cancelled + 144 issued. SD same gap. |
| M6 | CA address = many permits | ✅ Real, bounded | 33 SF addresses / 86 rows (3773 Sacramento = 6 permits sf:…0640-0645). |
| M7 | BD-tracking resolves company verbatim | ✅ Real | `BdResearchImport/Program.cs:6301` lacks `LeadOperator()` wrap (all peers have it) → composite-org risk. |
| Mi1 | Null CRM row Id=1 | ✅ Real | OpportunityId=92, all-null buyer/owner/region, by `ilalonde@…` (manual seed). |
| Mi3 | SD CSV provider present | ✅ Corroborated | `CA_SocrataSanDiego` CSV config confirmed. |
| Mi4 | Stale worker dedup comment | ✅ Real | `Worker/Program.cs:850-852` says weekly dedup runs; job retired. |

Strengths confirmed: frozen-anchor trigger `TR_CanonicalOrg_ProtectFrozenAnchor`
(mig 139); dedup job hard-retired (`CanonicalOrgDedupEnabled=false`); FK
completeness guard in the dedup CLI.

## Actioned 2026-06-19 (Claude, direct, verified)
- **M5 backlog**: soft-retired **250** dead-stage CA permits (SF 249 + SD 1).
  Active CA funnel now early-stage (filed/filing/triage/approved/Opened) + the
  issued/construction rows left pending Ian's strategy call. Reversible
  (`RetiredReason`).

## Remaining → `docs/BD-Fix-Prompt-2026-06-19.md` (Codex, then Claude verifies)
C1 dedup migration + write-path key + import idempotency (survivor map included);
M5 provider stage gate + config; M6 per-address rollup; M1 person identity key +
backfill; M7 LeadOperator wrap; M2 re-anchor; M3 app-lock; M4 allowlist split;
Mi1/Mi4 cleanups. Items flagged for Ian's decision: C1 engagement-identity
semantics, M5 issued-stage inclusion, M6 survivor rule, M1 key scheme.

## Foundation verdict
Not yet clean enough to make the BD data load-bearing for Arc-7 AI tools / Deltek
fusion. The dedup apparatus is materially better than the 2026-06-09 baseline,
but the live CRM has duplicate engagements, the contact graph has name-only
identity risk, and the CA funnel (now partly remediated) needs a permanent stage
gate. Close C1, M5 (source gate), and M1 before flipping the AI/Deltek phase on.
