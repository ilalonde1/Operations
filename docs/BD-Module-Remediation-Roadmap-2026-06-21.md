# BD Module Remediation Roadmap — 2026-06-21

Source of truth: `docs/BD-Module-GapAnalysis-2026-06-21.md` (Codex-produced, **verified by Claude against the live DB — every load-bearing number exact**). Goal: clean and solidify the BD module to production quality, **no regression**, every write gated.

## The gate (applies to EVERY write, no exceptions)
1. **Pre-flight / dry-run** — simulate the exact effect against the live DB (resolution map for ingests; `BdCanonicalDedup` plan for merges). The importer's own `--dry-run` is insufficient for resolution (it short-circuits the resolver) → use the deterministic resolution pre-flight (`output/preflight-org-resolution.ps1`).
2. **Review** — Ian sees the plan (match-vs-create, survivor-vs-loser) before commit.
3. **Commit** — apply.
4. **Verify + post-audit** — re-query; for merges, audit survivors (wrong SurvivorIds have bitten us twice — `feedback_honing_merge_audit`).

## Block 0.5 — Cold-archive the commodity-vendor bloat — ✅ **DONE + VERIFIED (2026-06-21)**
Segmentation proved only ~10.5% of 49,776 active orgs are connected to any KOR signal; the rest is CanadaBuys/BC-Bid procurement residue. **Archived 32,210** then **resurrected 273** (see warm-lead audit below) → **net 31,937 archived; warm set 49,776 → 17,839.** Archive predicate (final, tightened): Kind ∈ {Vendor, Subcontractor}, no Deltek ID, and **NO** tie to any of: active MPI role, CRM engagement, IntelSignal/Narrative/Work/Action, OpportunityInterestedFirms, Opportunity-buyer, NewsArticleOrgMention.
- **Warm-lead audit (2026-06-21):** scanned all 28 canonical-org-referencing columns. **Hard-verified 0 archived orgs have any MPI role (incl. retired) or CRM tie.** Initial sweep over-archived 273 with softer signals (intel narrative/work, tender-interest on 75 open opps, real firms mislabeled Vendor like RWDI/Jensen Hughes) → all 273 resurrected. Cold residue confirmed = orphan `OpportunityAwards.AwardedToCanonicalOrgId` (28,812 — the firehose itself), bids, permits.
- **Reversible by construction (no blackball):** the resolver's `FindByNormalizedNameAsync` / `LookupAliasAsync` do NOT filter `RetiredAtUtc`, and on any match call `UnretireAsync` (sets `RetiredAtUtc = NULL`). So any future opportunity/award/ingest that names an archived org **resurrects the same row** — no dup, no rejection. Proven live on sample #908 (found-while-archived → resurrected → re-archived).
- **Deliberately conservative:** `Unknown`/`Buyer`/`Developer`/`Architect`/`GC`/`Competitor`/`Client` untouched (fresh CA/northern targets live there). Dropped the "created >30d" guard — `CreatedAtUtc` was bulk-restamped (all May–Jun), unreliable as age.
- **Follow-ups:** (a) front-door — born-archive Vendor/Sub created via the two procurement firehose call sites (`SqlOpportunityAwardStore:217`, `BcBidUnverifiedBidResultsScraper:515`); NOT `KorPursuitDeltekSyncJob:132` (lost-to intel). (b) nightly backstop — extend `DataRetirementJob` with the same predicate behind a new `LowValueOrgArchiveEnabled` flag.

## Block 1 — Solidify the org graph (FOUNDATION; everything depends on it)
- **1a. Merge the dup clusters** — ✅ **DONE + VERIFIED (2026-06-21).** Tool plan = 98 pairs; reviewed all; excluded `legacy` (different firms); allowlisted 38 descriptor-variants (campaign file `dedup-non-similar-allowlist.d/north-cleanup-2026-06-21.csv`). **Committed 92/97; verified: active orgs 49,873→49,781 (−92), survivors present/losers gone, 0 new orphans.** **5 DEFERRED** (CitySpaces #4905, HSEA #71633, L7 #48014, Lime #70765, Wilden #25): rolled back safely on `UX_CrmEngagements_BdRelationship` collision (loser+survivor each hold a buyer+owner+region engagement) — **fix = reconcile the duplicate CRM engagement first, then re-merge.** No data lost; both rows intact.
- **1b. Reclassify the mislabels** — ✅ **DONE + VERIFIED (2026-06-21).** Of the original 45, several merged away in 1a; **40 reclassified** (role-derived; institutional owners — cities/counties/depts/universities/Microsoft/Stanford/Sun Life → **Buyer**, not Developer; clear devs/architects/GCs → their kind). **2 person-strings HELD** (#76990 CORRALES, #77005 HILL/HO/PANG) → moved to 1c name cleanup. After: only those 2 Vendor/Unknown remain in active MPI roles. **Extended 2026-06-21:** role-derived fix across the FULL warm set (incl. retired-project roles the rank-upgrade missed) — **41 more Kinds corrected** (AECOM/RJC/SMP/Falcon/Jarvis/H.H. Angus → Competitor, 6 architects → Architect, school/regional districts + Town of → Buyer, First Nations + developers → Developer). 3 skipped (gov ministry #74352 + 2 person-strings). Root cause: procurement-award path force-stamps `Vendor` and the rank-only upsert never downgrades → real firms stuck at Vendor.
- **1c. Split / clean the name-integrity messes** — ⏸ **CHARACTERIZED 2026-06-21, awaiting review (judgment-gated).** Broad detection = **641 active orgs** with name-integrity signals, but **only ~10 are in active MPI use** (the rest don't corrupt the project graph). Splits into THREE distinct problems:
  - **(i) Graph-impacting (~10, HIGH priority):** mostly JV-strings in live MPI roles — #54155 `Squamish Nation/Kingswood/Progressive`, #54845 `KPF/Adamson`, #54595, #54131 `Tercon/Consortium`, #54772, #54875, #54529, #68836 CSU, #76920 Pickard Chilton(note-bleed), + held person-string #77005. → apply JV policy (`project_jvstring_canonicalization`) per-case.
  - **(ii) Health-authority cluster — ✅ DONE + AUDITED (2026-06-21).** Consolidated **32 → 9** clean rows. 13 merges via `BdCanonicalDedup --pairs` (allowlisted campaign `ha-cleanup-2026-06-21.csv`) into the 5 rich survivors (Fraser #880, Interior #54977, Island #54979, Northern #54976, PHSA #72211); 11 MPI roles + 4 opps + 6 awards repointed; post-audit clean (0 lost people, 0 orphaned affiliations, 0 MPI orphans; the PHSA aff 12→8 was legit person-dedup). Retired 10 husk/blob rows (multi-HA/LMFM/JV husks + the 3 intel-bearing `#794/#695/#623` concatenations — 0 people orphaned, all also on active orgs). Judgment calls applied: Northern + PHSA bumped to **Client**; **BC Cancer Redevelopment kept separate** (distinct project owner); **Providence BC `Care` vs US `& Services` kept separate**.
  - **(iii) Description-bleed (~600 Vendor/Buyer rows):** name field absorbed award/contract text (`ULS Maintenance & Landscaping Awarded for Part 1…`, `Konica Minolta… (A registered vendor for…`). **This is a SOURCE extraction bug (Block 4 territory)** + a one-time truncation cleanup. Heuristic cut-point (`Awarded`/`(`/sentence) is risky on legit names (`Smith & Associates Ltd`) → review the truncation rule before applying. Not graph-urgent; affects brief quality.
- **1d. Repair the 16 MPI orphans** — 7 SE + 9 GC references point to absent org ids → null or re-resolve. *Verified: 7 + 9 = 16.*

## Block 2 — Pipeline integrity
- **2a. Add FKs on MPI `StructuralEngineerCanonicalOrgId` + `GeneralContractorCanonicalOrgId`** — ✅ **DONE + VERIFIED (2026-06-21).** Migration `259_MpiSeGcForeignKeys.sql` adds `FK_MPI_StructuralEngineerCanonicalOrg` + `FK_MPI_GeneralContractorCanonicalOrg` (NO_ACTION, mirrors Proponent/Architect). Applied to live DB; all 4 MPI org-role FKs now present, **trusted**, not disabled. 0 orphans at apply → validated clean. Orphan class now structurally impossible; dedup repoints SE/GC (FkTargets) before delete so merges are unaffected.
- **2b. Close the resolver dup-creation cycle** — ✅ **DONE + DEPLOYED (2026-06-21).** Migration `260` adds app-maintained `CanonicalOrg.FuzzyNormalizedName` (+ filtered index). Resolver now checks a **safe-tier** fuzzy key (`NormalizeForFuzzyMatch`: corporate-suffix + civic/school-district canonicalization; NOT the aggressive architects/group tier) AFTER strict and BEFORE create — folds intake into a **uniquely-matched** active org, returns null on ambiguity (verified: 37 active fuzzy-collisions, all correctly skipped). Aggressive variants stay human-reviewed in `BdCanonicalDedup`. `createArchived` no-resurrect guards preserved. Deployed `1.0.9668.1054`; backfill via `--backfill-fuzzy-key`. Dup backlog was already 0 at the aggressive tier; backfill then surfaced **47 `&`-vs-`and` fuzzy-collision pairs** the strict normalize had kept split (e.g. `Canada Paving & Construction`/`…and Construction`, `BC Ministry of Transportation &/and Transit`) — merged via `BdCanonicalDedup --pairs` (campaign `andamp-cleanup-2026-06-21.csv`) + 2 junk `??? ` rows retired. **Final: 0 active fuzzy-collisions, 0 MPI orphans, warm set 17,770.** Graph is now fully clean — ready for re-synthesis.

## Block 3 — Source health + missing sources
- **3a. Triage the 21 enabled-but-dead sources** (run "succeeds" but 0 rows — incl. both provincial MPI feeds). Per-source: fix or disable, with a row-count canary going forward.
- **3b. Fix the stalled Vancouver permit poll** (`Content-Length 81MB exceeds 52MB limit` → page/stream).
- **3c. Add missing sources** (human-gated onboarding): Surrey/Calgary/Edmonton/Victoria permit APIs, OpenNWT/GNWT, Yukon Bids & Tenders, Infrastructure BC, Northern Health, First Nations.

## Block 4 — Extraction completeness
- **4a. Fix the lossy importer tags** so SE/GC/seat actually persist (the 3.2% SE-coverage root cause) — incl. `indigenous` writing SE into schedule-notes instead of the SE column; harden `pipeline-seats` against null-overwrite of classifications.
- **4b. Widen `OpportunityCandidate`** to carry architect/GC/SE/contacts (typed observations + provenance, not raw-JSON overload).

## Block 5 — Ingest the northern research onto the clean graph
Re-do the project + org ingest (the rolled-back batch) with the pre-flight resolution gate + MPI-match (backfill existing, create only true net-new). Uses exact canonical names → zero dups.

## Block 6 — Enrich barren entities
Queue the barren/newly-seeded orgs through the org-brief enrichment pass (post-ingest enrichment is standard — `feedback_postingest_enrichment`).

---
**Execution order:** 1 → 2 → 3 → 4 → 5 → 6. Blocks 3/4 (code) can be drafted as Codex prompts and verified in parallel with 1/2 (data). **Currently executing: Block 1a (dedup dry-run).**
